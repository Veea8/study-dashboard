// ============================================================
// USER TODO — fill these in after completing the Azure app
// registration (see README.md). Neither value is secret; both
// are safe to commit to a public repo.
// ============================================================

// Application (client) ID from your Azure app registration.
// Leave empty until you have one — the app works without it,
// with Microsoft To Do sync disabled.
export const MS_CLIENT_ID = "";

// The SPA redirect URI you registered in Azure, e.g.
// "https://<username>.github.io/<repo>/".
// Leave empty to default to this page's own URL (works if that
// exact URL is what you registered).
export const MS_REDIRECT_URI = "";

// Name of the Microsoft To Do list the planner creates/uses.
export const DEFAULT_TODO_LIST_NAME = "CS Semester Planner";
